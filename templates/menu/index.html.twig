{% block sidemenu %}
    <div id="side-menu">
        <div class="side-menu-container">

            <div class="top-menu flx-row">
                <img src="/assets/imgs/logo.png" class="menu-logo" alt="logo">
                <button onclick="closeDiv()" id="button-close-menu">Menú <i class="fi-rr-cross"></i></button>
            </div>

            {% if app.user %}
            <div class="profile-menu-show flx-column">
                <div id="profile-pic">
                    <img src="https://www.habbo.es/habbo-imaging/avatarimage?user={{ app.user.username }}&action=std&direction=2&head_direction=2&img_format=gif&gesture=std&size=l" alt="Avatar">
                </div>
                <a href="/perfil/{{ app.user.username }}" class="profile-link">
                    <span id="photo-pseudo">{{ app.user.username }}</span>
                </a>
                <span id="photo-rank">{{ app.user.rank.name }}</span>
                <a href="/logout" class="menu--btn"><span>Cerrar sesión</span></a>
                <div class="user-purse-info">
                    <div class="purse-info">{{ app.user.points }} <span>puntos</span></div>
                </div>
            </div>
            {% else %}
                <div class="menu-auth-btn flx-column">
                    <a href="/login" class="menu--btn"><span>Iniciar sesión</span></a>
                    <a href="/registro" class="menu--btn"><span>Registrarse</span></a>
                </div>
            {% endif %}

            <nav>
                <ul>
                    {{ render(controller('App\\Controller\\MenuController::getMenu')) }}
                </ul>
            </nav>

        </div>
    </div>

    <script>
        function closeDiv() {
            document.getElementById('side-menu').style.display = "none";
        }
    </script>
{% endblock %}
